unit constants;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils;

const
  PIXELS_PER_CELL = 2;
  MAX_WIDTH = 300;
  MAX_HEIGHT = 300;
  INITIAL_WIDTH = 200;
  INITIAL_HEIGHT = 200;
  RANDOM_SPARSENESS = 10;
  TIMER_INTERVAL_UNIT = 5;

implementation

end.

